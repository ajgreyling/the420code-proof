# PROV-CCC.6 — Superseded Drafts Received, One Loss Recorded, and the Unblinding

**The 420 Code · Studio G, Strand, Cape Town · Record date: 2026-08-11.**
**References: PROV-CCC.1–5, digests as previously published, never edited.**
**Status: canonical text for hashing. Amendments to PROV-CCC.7.**

---

## 1 — Author's copies of the superseded drafts: received and hashed

- `AP_CCC_The_Snap_DRAFT_v0_2.docx` — SHA-256 **d4aba6168781b652a0e2178f695832c017afa287c3f2c38ba9b423dce12ab6a9**. Banner verified: "Draft v0.2 — production draft for the author's ruling."
- `AP44_The_Snap_DRAFT_v0_3.docx` — SHA-256 **0ce22daf7aa77c6c64c87b8e995aae687a791a431262b8760ee50f7f6c922b69**. Banner verified: "Draft v0.3 … Number ruled: AP44."

Both are addendum files to the 2026-08-11 transfer bundle: the verifier commits them into `ap44/superseded/` beside v0.1, v0.5, and v0.6, and appends their lines to the manifest.

## 2 — Draft v0.4: lost, and recorded honestly

The author's copy of v0.4 is lost, and the build environment retained only the evolved current script. **No reconstruction is offered as the artifact.** The loss costs the chain nothing evidentially, because v0.4's content is fully determined by logged deltas that exist in two independent places: PROV-CCC.2 §5 (the Stage 10 log) and the T1 transcript (the patch commands verbatim). Specifically: v0.4 = v0.3 plus one precision sentence in §10 (locating the count's first move inside the measurement-laden opening analysis), the banner and end-line version bump, and the provenance-reference update — nothing else; §9 untouched; zero numerical motion. The audit path v0.3 → v0.5 is closed by those logs. A lost intermediate whose delta is fully on the record is a documented gap, not a hole.

## 3 — T1-final: exported by the author, digest pending

The author reports the production-thread export made 2026-08-11. The file has not yet passed through hashing hands. Slot **T1-final** remains formally open: the verifier hashes the file on receipt and records the digest in the repository manifest addendum; PROV-CCC.7 (or the manifest commit itself) closes the slot. The thread's content through 21:08:55 on 10 Aug is already sealed under T1 (PROV-CCC.4).

## 4 — Tier 2 milestone: the freeze held, and the key is issued

The verifier's coded table was frozen at repository commit **`d8929cf`** *before* any request for the corpus's values — the choreography of the handover protocol held exactly as written. The unblinding key (`UNBLINDING_KEY_KS-CCC4.md`, SHA-256 in its sidecar) is issued this date, containing: the structural and realised values, the per-experiment η and r columns, both Tier 1 executions' results, and the pre-registered criteria for KS-CCC.4a and 4b restated with the F-invariance limit binding. The author's ruling accompanies it on the record: **the analysis runs exactly as written — whichever way it cuts.** All commits after `d8929cf` are post-unblinding and are marked as such.

## 5 — Standing

Open after this note: the T1-final digest (§3), and the lock ruling on AP44 v0.6 — the author's read, still his alone. The Tier 2 analysis proceeds under the key. Nothing else is owed.

*This work is published for free, forever.*
