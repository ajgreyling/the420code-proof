# READ THIS FIRST — AP44 bundle for AJ, 2026-08-10

**Start with `AJ_Handover_and_Tier2_Protocol.md`. Its read-order warning is
load-bearing: commit and anchor first, do the blind Tier 2 coding second,
and only then open the physics papers (`ap44/`, `appendix-c/`).**

Contents: the AP44 draft (v0.6, formatting conformed to the AP02 house
standard — content identical to v0.5, which is retained in `superseded/`
with v0.1; G supplies v0.2–v0.4 from his downloads separately), the Tier 1
working paper with dataset/script/figure, the full provenance chain
PROV-CCC.1–4 with the session record, the two extracted conversation
artifacts (T1, T2), the first analog-archive photographs, and the escrow
folder (read its README — never public).

Author rulings recorded 2026-08-10: T1 publishes as-is (option a — G's
ruling); the raw export zips remain escrow-only regardless (account
security, not preference). Still to come from G: the morning photo set
(M1b), a final post-session export (T1-final), and the v0.2–v0.4 copies —
all consolidated in PROV-CCC.5.

Verify everything: `sha256sum -c BUNDLE_MANIFEST.txt`
